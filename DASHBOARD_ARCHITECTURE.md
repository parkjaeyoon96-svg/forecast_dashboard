# 대시보드 아키텍처 및 처리 절차

## 📋 목차
1. [전체 처리 흐름](#전체-처리-흐름)
2. [파일 구조 및 관계](#파일-구조-및-관계)
3. [데이터 로드 순서](#데이터-로드-순서)
4. [전역 변수 매핑](#전역-변수-매핑)
5. [UI 업데이트 함수](#ui-업데이트-함수)

---

## 전체 처리 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. 페이지 로드 (Dashboard.html)                                  │
│    - URL 파라미터 읽기: ?date=20251124                           │
│    - 전역 변수 초기화 (window.overviewPL, window.brandKPI 등)    │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. 자동 실행: loadAllDashboardData()                            │
│    - 날짜 기반 경로 생성: /data/20251124/                        │
│    - 모든 JSON 파일 병렬 로드 (Promise.all)                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. JSON 파일 로드 (병렬 처리)                                    │
│    ┌─────────────────────────────────────────────────────┐     │
│    │ Phase 1: Overview 데이터 (5개 파일)                  │     │
│    │ - overview_kpi.json                                  │     │
│    │ - overview_by_brand.json                            │     │
│    │ - overview_pl.json                                  │     │
│    │ - overview_waterfall.json                            │     │
│    │ - overview_trend.json                                │     │
│    └─────────────────────────────────────────────────────┘     │
│    ┌─────────────────────────────────────────────────────┐     │
│    │ Phase 2: 브랜드별 데이터 (9개 파일)                   │     │
│    │ - brand_kpi.json                                     │     │
│    │ - brand_pl.json                                     │     │
│    │ - channel_profit_loss.json                          │     │
│    │ - radar_chart.json                                  │     │
│    │ - weekly_trend.json                                 │     │
│    │ - stock_analysis.json                               │     │
│    │ - treemap.json                                     │     │
│    │ - brand_plan.json                                  │     │
│    │ - ai_insights/insights_data_20251124.json          │     │
│    └─────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ 4. 전역 변수에 데이터 저장                                       │
│    - window.overviewPL = overviewPL                            │
│    - window.brandKPI = brandKPI                                │
│    - window.weeklySalesTrend = weeklyTrend (변환 후)           │
│    - ... (기타 전역 변수들)                                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ 5. DOMContentLoaded 이벤트                                      │
│    - loadDataFile() 호출                                        │
│    - loadAllDashboardData() 완료 대기                           │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ 6. UI 업데이트 함수 호출 (순차 실행)                             │
│    - updateOverviewPL()      → 전체현황 손익계산서              │
│    - updateBrandPL()          → 브랜드별 손익계산서              │
│    - updateWeeklyTrendChart() → 주차별 매출 추세                 │
│    - updateBrandChannels()    → 채널별 손익                     │
│    - updateBrandView()        → 브랜드 KPI 카드                 │
│    - renderInsights()         → AI 인사이트                    │
│    - initTreemap()            → 트리맵 차트                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ 7. 사용자 인터랙션 대기                                          │
│    - 브랜드 버튼 클릭 → updateBrandView() 등 호출                │
│    - 섹션 전환 → 해당 섹션의 차트/테이블 업데이트               │
│    - 필터 변경 → 차트 데이터 재계산 및 렌더링                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 파일 구조 및 관계

### 📁 디렉토리 구조
```
public/
├── Dashboard.html                    # 메인 HTML 파일 (모든 로직 포함)
└── data/
    └── 20251124/                     # 날짜별 데이터 폴더
        ├── overview_kpi.json         # 전체현황 KPI
        ├── overview_by_brand.json    # 브랜드별 매출/이익
        ├── overview_pl.json          # 전체현황 손익계산서 ⭐
        ├── overview_waterfall.json   # 워터폴 차트
        ├── overview_trend.json       # 누적 추이
        ├── brand_kpi.json            # 브랜드별 KPI ⭐
        ├── brand_pl.json             # 브랜드별 손익계산서
        ├── channel_profit_loss.json  # 채널별 손익
        ├── radar_chart.json          # 레이더 차트
        ├── weekly_trend.json         # 주차별 매출 추세 ⭐
        ├── stock_analysis.json       # 재고 분석
        ├── treemap.json              # 트리맵 차트
        ├── brand_plan.json           # 브랜드 계획
        └── ai_insights/              # AI 인사이트
            ├── insights_data_20251124.json
            ├── ai_insights_MLB_20251124.json
            ├── ai_insights_DISCOVERY_20251124.json
            └── ... (브랜드별 파일)
```

### 🔗 파일 관계도

```
Dashboard.html
    │
    ├─→ loadAllDashboardData()
    │       │
    │       ├─→ /data/{날짜}/overview_pl.json
    │       │       └─→ window.overviewPL
    │       │               └─→ updateOverviewPL()
    │       │                       └─→ #overview-pl 테이블 업데이트
    │       │
    │       ├─→ /data/{날짜}/brand_kpi.json
    │       │       └─→ window.brandKPI
    │       │               └─→ window.brandData (변환)
    │       │                       └─→ updateBrandView()
    │       │                               └─→ 브랜드 KPI 카드 업데이트
    │       │
    │       ├─→ /data/{날짜}/weekly_trend.json
    │       │       └─→ window.weeklySalesTrend (구조 변환)
    │       │               └─→ updateWeeklyTrendChart()
    │       │                       └─→ #weeklyTrendChart 차트 렌더링
    │       │
    │       ├─→ /data/{날짜}/channel_profit_loss.json
    │       │       └─→ window.channelProfitLossData
    │       │               └─→ updateBrandChannels()
    │       │                       └─→ 채널별 손익 테이블 업데이트
    │       │
    │       ├─→ /data/{날짜}/brand_pl.json
    │       │       └─→ window.brandPLData
    │       │               └─→ updateBrandPL()
    │       │                       └─→ 브랜드별 손익계산서 테이블
    │       │
    │       ├─→ /data/{날짜}/treemap.json
    │       │       └─→ window.channelItemSalesData
    │       │               └─→ initTreemap()
    │       │                       └─→ Plotly 트리맵 렌더링
    │       │
    │       └─→ /data/{날짜}/ai_insights/insights_data_*.json
    │               └─→ window.insightsData
    │                       └─→ renderInsights()
    │                               └─→ AI 인사이트 표시
    │
    └─→ 사용자 인터랙션
            │
            ├─→ 브랜드 버튼 클릭
            │       └─→ updateBrandView()
            │       └─→ updateBrandPL()
            │       └─→ updateWeeklyTrendChart()
            │       └─→ updateBrandChannels()
            │
            ├─→ 섹션 전환 (전체현황 ↔ 브랜드별 분석)
            │       └─→ 해당 섹션의 모든 차트/테이블 업데이트
            │
            └─→ 필터 변경 (채널, 주차/누적 등)
                    └─→ 차트 데이터 재계산 및 렌더링
```

---

## 데이터 로드 순서

### Phase 1: Overview 데이터 (전체현황)
```javascript
// 1단계: Overview 관련 JSON 파일 병렬 로드
Promise.all([
  fetch('/data/20251124/overview_kpi.json'),        // 전체 KPI
  fetch('/data/20251124/overview_by_brand.json'),   // 브랜드별 매출/이익
  fetch('/data/20251124/overview_pl.json'),         // 전체 손익계산서 ⭐
  fetch('/data/20251124/overview_waterfall.json'), // 워터폴 차트
  fetch('/data/20251124/overview_trend.json')       // 누적 추이
])
```

**저장 위치:**
- `window.overviewKPI` ← `overview_kpi.json`
- `window.by_brand` ← `overview_by_brand.json`
- `window.overviewPL` ← `overview_pl.json` ⭐
- `window.waterfallData` ← `overview_waterfall.json`
- `window.cumulativeTrendData` ← `overview_trend.json`

### Phase 2: 브랜드별 데이터
```javascript
// 2단계: 브랜드별 JSON 파일 병렬 로드
Promise.all([
  fetch('/data/20251124/brand_kpi.json'),              // 브랜드별 KPI ⭐
  fetch('/data/20251124/brand_pl.json'),               // 브랜드별 손익계산서
  fetch('/data/20251124/channel_profit_loss.json'),   // 채널별 손익
  fetch('/data/20251124/radar_chart.json'),            // 레이더 차트
  fetch('/data/20251124/weekly_trend.json'),           // 주차별 매출 추세 ⭐
  fetch('/data/20251124/stock_analysis.json'),         // 재고 분석
  fetch('/data/20251124/treemap.json'),                // 트리맵
  fetch('/data/20251124/brand_plan.json'),             // 브랜드 계획
  fetch('/data/20251124/ai_insights/insights_data_20251124.json') // AI 인사이트
])
```

**저장 위치:**
- `window.brandKPI` ← `brand_kpi.json` ⭐
- `window.brandPLData` ← `brand_pl.json`
- `window.channelProfitLossData` ← `channel_profit_loss.json`
- `window.radarChartData` ← `radar_chart.json`
- `window.weeklySalesTrend` ← `weekly_trend.json` (구조 변환 후) ⭐
- `window.brandStockMetadata` ← `stock_analysis.json`
- `window.channelItemSalesData` ← `treemap.json` (구조 변환 후)
- `window.brandPlanData` ← `brand_plan.json`
- `window.insightsData` ← `insights_data_20251124.json`

---

## 전역 변수 매핑

### 📊 전체현황 관련
| JSON 파일 | 전역 변수 | 용도 | 업데이트 함수 |
|-----------|----------|------|--------------|
| `overview_kpi.json` | `window.overviewKPI` | 전체 KPI 지표 | - |
| `overview_by_brand.json` | `window.by_brand` | 브랜드별 매출/이익 | - |
| `overview_pl.json` | `window.overviewPL` | **전체 손익계산서** | `updateOverviewPL()` ⭐ |
| `overview_waterfall.json` | `window.waterfallData` | 워터폴 차트 | - |
| `overview_trend.json` | `window.cumulativeTrendData` | 누적 추이 차트 | - |

### 🏷️ 브랜드별 분석 관련
| JSON 파일 | 전역 변수 | 용도 | 업데이트 함수 |
|-----------|----------|------|--------------|
| `brand_kpi.json` | `window.brandKPI` | 브랜드별 KPI | `updateBrandView()` ⭐ |
| `brand_pl.json` | `window.brandPLData` | 브랜드별 손익계산서 | `updateBrandPL()` |
| `channel_profit_loss.json` | `window.channelProfitLossData` | 채널별 손익 | `updateBrandChannels()` |
| `radar_chart.json` | `window.radarChartData` | 레이더 차트 | `updateRadarStaticOrDynamic()` |
| `weekly_trend.json` | `window.weeklySalesTrend` | **주차별 매출 추세** | `updateWeeklyTrendChart()` ⭐ |
| `stock_analysis.json` | `window.brandStockMetadata` | 재고 분석 | `updateInventoryChart()` |
| `treemap.json` | `window.channelItemSalesData` | 트리맵 차트 | `initTreemap()` |
| `brand_plan.json` | `window.brandPlanData` | 브랜드 계획 | - |
| `ai_insights/insights_data_*.json` | `window.insightsData` | AI 인사이트 | `renderInsights()` |

### 🔄 데이터 변환 과정

#### 1. `brand_kpi.json` → `window.brandData`
```javascript
// 원본: window.brandKPI[브랜드명] = { revenue, directProfit, ... }
// 변환: window.brandData[브랜드명] = { current: {...}, revenue: {...}, profit: {...} }
// 용도: 브랜드 KPI 카드 표시
```

#### 2. `weekly_trend.json` → `window.weeklySalesTrend`
```javascript
// 원본: { weeklySalesTrend: { 브랜드: { 채널: { current: [...], previous: [...] } } } }
// 변환: { weeks: [...], brands: [...], rawData: [...] }
// 용도: 주차별 매출 추세 차트
```

#### 3. `treemap.json` → `window.channelItemSalesData`
```javascript
// 원본: { channelTreemapData: { byBrand: { ... } } }
// 변환: { 브랜드코드: { 채널명: { 아이템명: 매출 } } }
// 용도: Plotly 트리맵 차트
```

---

## UI 업데이트 함수

### 📍 호출 순서 (데이터 로드 완료 후)

```
loadAllDashboardData() 완료
    ↓
loadDataFile() 내부
    ↓
┌─────────────────────────────────────────┐
│ 1. updateOverviewPL()                   │
│    - window.overviewPL 사용              │
│    - #overview-pl 테이블 업데이트        │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 2. updateBrandPL()                      │
│    - window.brandPLData 사용             │
│    - 브랜드별 손익계산서 테이블           │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 3. updateWeeklyTrendChart()              │
│    - window.weeklySalesTrend 사용        │
│    - #weeklyTrendChart 차트 렌더링       │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 4. updateBrandChannels()                │
│    - window.channelProfitLossData 사용   │
│    - 채널별 손익 테이블                   │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 5. updateBrandView()                    │
│    - window.brandData 사용               │
│    - 브랜드 KPI 카드 업데이트             │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 6. renderInsights()                     │
│    - window.insightsData 사용            │
│    - AI 인사이트 표시                    │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 7. initTreemap()                       │
│    - window.channelItemSalesData 사용   │
│    - Plotly 트리맵 렌더링               │
└─────────────────────────────────────────┘
```

### 🔄 사용자 인터랙션 시 호출

#### 브랜드 버튼 클릭 시
```javascript
브랜드 버튼 클릭
    ↓
currentBrand 변경
    ↓
┌─────────────────────────────────────────┐
│ 순차 실행:                               │
│ 1. renderInsights()                     │
│ 2. updateBrandView()                    │
│ 3. updateBrandPL()                      │
│ 4. initBrandCharts()                    │
│ 5. updateBrandCharts()                  │
│ 6. renderItemDiffTable()                │
│ 7. updateInventoryChart()               │
│ 8. updateWeeklyTrendChart() ⭐          │
│ 9. updateBrandChannels()                 │
└─────────────────────────────────────────┘
```

#### 섹션 전환 시
```javascript
섹션 전환 (전체현황 ↔ 브랜드별 분석)
    ↓
해당 섹션의 모든 차트/테이블 업데이트
    ↓
브랜드별 분석 섹션인 경우:
    - updateBrandView()
    - updateBrandPL()
    - updateWeeklyTrendChart() ⭐
    - updateBrandChannels()
    - renderItemDiffTable()
```

---

## 주요 함수 설명

### `loadAllDashboardData()`
- **위치**: `<head>` 섹션 (페이지 로드 시 자동 실행)
- **역할**: 모든 JSON 파일을 병렬로 로드하고 전역 변수에 저장
- **반환**: Promise (비동기)

### `loadDataFile(dateStr)`
- **위치**: `DOMContentLoaded` 이벤트 핸들러 내부
- **역할**: `loadAllDashboardData()` 완료 후 UI 업데이트 함수 호출
- **호출 시점**: DOM이 준비된 후

### `updateOverviewPL()`
- **데이터 소스**: `window.overviewPL` (← `overview_pl.json`)
- **업데이트 대상**: `#overview-pl` 테이블
- **호출 시점**: 데이터 로드 완료 후, 섹션 전환 시

### `updateWeeklyTrendChart()`
- **데이터 소스**: `window.weeklySalesTrend` (← `weekly_trend.json`, 구조 변환 후)
- **업데이트 대상**: `#weeklyTrendChart` (Chart.js)
- **호출 시점**: 데이터 로드 완료 후, 브랜드 변경 시, 채널 필터 변경 시

### `updateBrandView()`
- **데이터 소스**: `window.brandData` (← `brand_kpi.json` 변환 후)
- **업데이트 대상**: 브랜드 KPI 카드 (매출, 이익, 진척율 등)
- **호출 시점**: 데이터 로드 완료 후, 브랜드 변경 시

---

## 데이터 흐름 요약

```
JSON 파일
    ↓ (fetch)
전역 변수 (window.*)
    ↓ (함수 호출)
UI 업데이트 함수
    ↓ (DOM 조작)
화면 표시
```

---

## 주의사항

1. **날짜 파라미터**: URL에 `?date=20251124` 형식으로 날짜를 전달해야 함
2. **비동기 처리**: 모든 데이터 로드는 `Promise.all()`로 병렬 처리됨
3. **에러 처리**: JSON 파일이 없어도 에러 없이 계속 진행 (null 반환)
4. **데이터 변환**: 일부 JSON은 사용하기 전에 구조 변환이 필요함
5. **전역 변수**: 모든 데이터는 `window` 객체에 저장되어 어디서든 접근 가능

---

## 디버깅 팁

### 콘솔 로그 확인
```javascript
// 데이터 로드 상태 확인
console.log('overviewPL:', !!window.overviewPL);
console.log('brandKPI:', Object.keys(window.brandKPI || {}));
console.log('weeklySalesTrend:', !!window.weeklySalesTrend);
```

### 네트워크 탭 확인
- `/data/20251124/*.json` 파일들이 모두 200 OK로 로드되는지 확인
- 실패한 파일이 있으면 해당 파일이 존재하는지 확인

### 함수 호출 확인
- 브라우저 콘솔에서 `typeof updateOverviewPL` 확인 (함수 정의 여부)
- `updateOverviewPL()` 직접 호출하여 테이블이 업데이트되는지 확인



















